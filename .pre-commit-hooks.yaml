- id: fix-databricks-magic
  name: Fix bare magic commands in Databricks notebooks
  entry: fix-databricks-magic
  language: python
  types: [python]
  description: >
    Reports bare magic commands (%pip, !, etc.) that need # MAGIC prefix
    in Databricks .py-format notebooks. Pass --fix to rewrite files.
